name: Buscador Diecast Automatico

on:
  workflow_dispatch:

  schedule:
    # 08:00 até 11:30 no horário de Brasília
    - cron: "0,30 11-14 * * *"

    # 12:00 até 19:45 no horário de Brasília
    - cron: "0,15,30,45 15-22 * * *"

    # 20:00 até 22:45 no horário de Brasília
    - cron: "0,15,30,45 23,0,1 * * *"

    # 23:00 no horário de Brasília
    - cron: "0 2 * * *"

permissions:
  contents: write

jobs:
  buscar:
    runs-on: ubuntu-latest

    steps:
      - name: Baixar projeto
        uses: actions/checkout@v6

      - name: Configurar Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Instalar dependencias
        run: pip install requests

      - name: Buscar ofertas
        env:
          SERPAPI_KEY: ${{ secrets.SERPAPI_KEY }}
        run: python main.py

      - name: Salvar historico e links
        run: |
          git config user.name "Buscador Diecast"
          git config user.email "actions@github.com"

          git add ofertas_vistas.json links_para_afiliado.txt

          if git diff --cached --quiet; then
            echo "Nenhuma alteracao para salvar"
          else
            git commit -m "Atualiza ofertas e links para afiliado"
            git push
          fi
